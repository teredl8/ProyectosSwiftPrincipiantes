//
//  Prestamo.swift
//  Prestamos laboratorio
//
//  Created by Ios lab on 16/03/23.
//

import Foundation
import CoreData
import UIKit

class PrestamoEquipos{
    /*
    public static let shared = PrestamoEquipos()
    
    //Conexión con la base de datos
    func contexto() -> NSManagedObjectContext {
        let delegate = UIApplication.shared.delegate as! AppDelegate
        return delegate.persistentContainer.viewContext
    }
    
    func guardarInformacionPrestamo(equipo: String, fecha: Date, idPrestamo: String, numeroEquipo: String) {
        let context = contexto()
        let entidadEquipo = NSEntityDescription.insertNewObject(forEntityName: "Prestamo", into: context) as! Prestamo
        entidadEquipo.equipo = equipo
        entidadEquipo.fecha = fecha
        entidadEquipo.id_prestamo = idPrestamo
        entidadEquipo.numero = numeroEquipo
        
        do{
            try context.save()
            print("Se guardó la información con éxito")
        } catch let error as NSError {
            print("No fue posible guardar la información", error.localizedDescription)
        }
        
    }*/
    
}
